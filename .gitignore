# ESP-IDF build output
build/
sdkconfig
sdkconfig.old
sdkconfig.ci

# ESP-IDF component manager
managed_components/
dependencies.lock

# Editor / IDE
.vscode/
.idea/
*.swp

# macOS
.DS_Store

# Python (idf tooling helpers)
__pycache__/
*.pyc

# Wrangler CLI cache
.wrangler/

# OTA signing: never commit the private key (CI decodes it from a secret).
ota_signing_key.pem
*_signing_key.pem
crowmotion-keys/
